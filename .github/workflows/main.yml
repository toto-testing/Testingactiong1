name: Octo Organization CI

on:
  push:
  workflow_dispatch:
  pull_request:
    branches: [ $default-branch ]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: DEV
    steps:
      - uses: actions/checkout@v4
      - run: ./.github/scripts/test.sh
        shell: bash
      - name: Run a one-line script
        run: echo Hello from Toto-testing Organization
